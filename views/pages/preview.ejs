<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>Preview Chippo URL</title>
    <% include ../partials/head %>
  </head>
  <body>
    <div class="container">
      <% include ../partials/tabbar %>
      <section class="hero is-medium is-success is-bold">
        <div class="hero-body">
          <div class="container">
            <% if (type === "success") { %>
            <h1 class="title">
              Preview
            </h1>
            <h2 class="subtitle">
              <div>
                URL:
                <a href="<%= chippo.url %>" target="_blank"
                  ><%= chippo.url %></a
                >
              </div>
              <div>
                Chippo:
                <a href="<%= chippo.fullAlias %>" target="_blank"
                  ><%= chippo.fullAlias %></a
                >
              </div>
            </h2>
            <% } else if (type === "failure") { %>
            <h1 class="title">
              Chippo Failed
            </h1>
            <h2 class="subtitle">
              <div>
                URL:
                <a href="<%= input.url %>" target="_blank"><%= input.url %></a>
              </div>
              <div>Chipo: <%= input.alias %> is not available</div>
            </h2>
            <% } %>
          </div>
        </div>
      </section>
      <% include ../partials/footer %>
    </div>
  </body>
</html>
